apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-deployment-green
  labels:
    version: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-app-green
  template:
    metadata:
      labels:
        app: flask-app-green
    spec:
      containers:
        - name: flask-app
          image: anhaid/flask-app:${{ github.event.client_payload.image }}
          ports:
            - containerPort: 8080